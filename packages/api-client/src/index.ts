// The generated surface. `schema.ts` is produced by `pnpm gen:api` from
// specs/001-master-career-profile/contracts/openapi.yaml and is committed, so
// continuous integration can verify it matches the contract.
export type { paths, components } from "./schema";
