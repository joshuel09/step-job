# Copy to .env and adjust. Never commit a filled-in .env.
DATABASE_URL=postgresql+psycopg://stepjob:stepjob@localhost:5432/stepjob
ENVIRONMENT=local

# Host ports for local services; override if these are already taken.
POSTGRES_PORT=5432
REDIS_PORT=6379
